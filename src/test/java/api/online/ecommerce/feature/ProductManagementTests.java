package api.online.ecommerce.feature;

import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;

public class ProductManagementTests {
    @Test
    @DisplayName("Given a list of cartItems When adding a product to the cart Then the cart should be populated with the relevant product")
    public void testCustomerCanAddProductToCart() {

    }

    @Test
    @DisplayName("Given a populated cart When removing a product from the cart Then the product removed should not be displayed in the cart")
    public void testCustomerCanRemoveProductFromCart() {
    }

    @Test
    @DisplayName("Given a populated cart When viewing the cart Then the cart should display the relevant cartItems in the cart")
    public void testCustomerCanViewProductsInCart() {

    }

    @Test
    @DisplayName("Given an no cartItems are added to the cart When viewing the cart Then the cart should be empty")
    public void testCustomerHasNoProductsInCart() {

    }
}
